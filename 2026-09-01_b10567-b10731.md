# llama.cpp 版本差異報告 b10566 → b10731

【產出時間】2026-09-01（第三版，覆蓋同日同範圍前兩版）
【清單版本】`config/watchlist-changelog.md` 最新異動日 2026-09-01
           （含 R3、jinja/chat template、Qualcomm Glymur、類別2 實際 model
             清單、**角色 prompt 的「哪些 backend 才是我們的」判讀規則**）

【覆蓋量】本次掃描 126 筆 release，36 筆入選（⚠️風險 12 筆／✨機會 24 筆）

【跳版成本】從 b10566 跳到 b10731：**3 條必須先驗**、**8 條要決定要不要採用**

---

## ⚠️ 風險

### 🔴 需要動手（改腳本或驗證）

```
【來源】b10594 | ba8e0ed | https://github.com/ggml-org/llama.cpp/pull/26692
【召回方式】模糊主題 R1
【類型】⚠️風險　【等級】🔴
【summary】device_info 迴圈改為僅在 verbosity 達 TRACE 時執行；該迴圈會建立
          GPU context 並查詢各裝置記憶體（CUDA 上配置 550MB VRAM）
【判讀依據】原文："the log verbosity must be set to LOG_LEVEL_TRACE. If it's
          not, including in the default configuration, the contexts get
          created, memory sizes get queried, then the log function quietly
          discards the data"
          **backend 判定：第2步——這個改動在 `common/` 裡，backend-agnostic。**
          文字提到 CUDA，但那只是舉例說明成本；我們走 Vulkan/CPU/OpenVINO
          一樣吃到這個行為。
          我們的腳本靠 --list-devices 輸出的 (N MiB, N MiB free) 偵測 VRAM，
          啟動時帶 -lv 3。若記憶體查詢在非 TRACE 下被跳過，regex 抓不到數字
          → maxMB=0 → 完全不帶 -ngl（fail-open）→ 小 VRAM 機台會嘗試全部
          offload。符合 R1 Counts(→🔴)
```

```
【來源】b10677 | b387ddf | https://github.com/ggml-org/llama.cpp/pull/27812
【召回方式】精確關鍵字（Vulkan，類別4）
【類型】⚠️風險　【等級】🔴
【summary】修正 ggml_vk_graph_optimize 漏掉 view-alias 依賴，導致節點跨
          aliased 讀寫被重排
【判讀依據】原文："silently wrong tokens under greedy decoding, different
          output on every server start, and invalid speculative-decoding
          acceptance, with nothing logged. Hits Qwen3.8's recurrent state
          (and any model with view-aliased state) on AMD and NVIDIA Vulkan"
          **backend 判定：第3步——Vulkan 是我們 x64 機台的路徑。**
          這是我們現在釘的版本就可能有的**靜默錯誤輸出**，且每次啟動結果
          不同、無任何 log。本期最該優先確認的一筆
```

```
【來源】b10598 | 56db501 | https://github.com/ggml-org/llama.cpp/pull/27594
【召回方式】模糊主題 R3
【類型】⚠️風險　【等級】🔴
【summary】mtmd 改用 pillow 精確的 resize 演算法，並修正各 model 的
          resize_algo
【判讀依據】原文："use pillow-accurate algo, correct resize_algo for all
          models"
          **backend 判定：第2步——mtmd 的影像預處理是 backend-agnostic。**
          我們偵測到 mmproj 就一律走多模態路徑（gemma-4-26B 有
          mmproj-BF16.gguf）。resize 演算法改變會直接改變輸出，而**沒有
          任何參數變動、沒有任何錯誤訊息**。符合 R3 Counts(→🔴)
```

### 🟡 留意

```
【來源】b10620 | 81191af | PR: (無)
【召回方式】內容不明（Stage 1 標記）
【類型】⚠️風險　【等級】🟡
【summary】（內容不明：sync : ggml，無 PR 編號）
【判讀依據】節錄僅有 "sync : ggml" 兩個字、無 PR 編號。這類 bulk merge 可能
          攜帶大量 kernel/後端/記憶體改動，但 release body 不提供任何訊號。
          **不假裝判讀得出來** → 需人工展開該 commit range 確認
```

```
【來源】b10700 | bebc935 | https://github.com/ggml-org/llama.cpp/pull/27969
【召回方式】精確關鍵字（類別5，參數定義變動）
【類型】⚠️風險　【等級】🟡
【summary】--tensor-read-lazy 改名為 --lazy-mode，新增 -lzm 短旗標
【判讀依據】backend-agnostic（arg parsing）。該參數本期才新增、我們尚未使用，
          目前無影響。列 🟡 的理由：證實 llama.cpp 會改名參數，且短旗標命名
          空間在擴張——我們依賴 `-m` / `-c` / `-lv` 的短形式
```

```
【來源】b10690 | bdf3955 | https://github.com/ggml-org/llama.cpp/pull/27967
【召回方式】精確關鍵字（KV cache，類別4）
【類型】⚠️風險　【等級】🟡
【summary】僅在 k_rot tensor 已配置 buffer 時複製 Hadamard 矩陣，避免未量化
          K cache 於 context shift 時 crash
【判讀依據】backend-agnostic（llama.cpp core memory）。我們不帶 -ctk，用預設
          的未量化 K cache，且帶 -c 65536，長對話可能觸發 context shift。
          需確認我們釘的版本是否在此修復之前
```

```
【來源】b10681 | 77f132c | https://github.com/ggml-org/llama.cpp/pull/27925
【召回方式】精確關鍵字（Vulkan，類別4）
【類型】⚠️風險　【等級】🟡
【summary】Vulkan mul_mat_id 改為 pad K 而非 pad N
【判讀依據】原文："we indirect the row index through a shared memory lookup
          table which avoids any OOB row coordinate. But that callback
          doesn't bounds check K, so we actually need K padding instead"
          **backend 判定：第3步——Vulkan 是我們的路徑。**
          `mul_mat_id` 是 MoE 的路徑，而我們跑的 gemma-4-26B-**A4B** 是 MoE。
          這是補上缺失的 K 邊界保護，屬我們現在釘的版本就存在的隱患
```

```
【來源】b10639 | 5e6a37c | https://github.com/ggml-org/llama.cpp/pull/27726
【召回方式】精確關鍵字（Vulkan，類別4）
【類型】⚠️風險　【等級】🟡
【summary】Vulkan warptile 原假設 warp size <= 64，對更大 warp 加入 clamp
          workaround
【判讀依據】**backend 判定：第3步——Vulkan 是我們的路徑。**
          這是相容性 workaround 而非最佳化，暗示某些裝置的 warp size 超出
          原假設。未點名具體硬體，所以不確定是否含我們的機台
```

```
【來源】b10630 | d222767 | https://github.com/ggml-org/llama.cpp/pull/26077
【召回方式】精確關鍵字（KleidiAI，類別4 後端driver）
【類型】⚠️風險　【等級】🟡
【summary】重整 KleidiAI 建置系統整合，加入 fp16 guard，並修正 cmake caching
【判讀依據】**backend 判定：第3步——我們 Arm 機台跑 `win-cpu-arm64` binary，
          ARM CPU 路徑是我們的**，而 KleidiAI 正是該路徑的優化程式庫。
          建置整合改動會影響**它到底有沒有被編進官方 release binary**。
          我們不自行 build，所以要確認的是：新版官方 arm64 binary 是否仍
          啟用 KleidiAI（否則 Arm 機台效能會靜默退步）
```

```
【來源】b10585 | d9f918d | https://github.com/ggml-org/llama.cpp/pull/27511
【召回方式】精確關鍵字（類別5，jinja）
【類型】⚠️風險　【等級】🟡
【summary】新增 common/json 抽象層，server 與 jinja 全面改用，含一處 server
          crash 修復
【判讀依據】backend-agnostic（`common/`）。我們不帶 --jinja，chat template
          完全吃 llama.cpp 預設行為。大規模重構觸及 jinja 路徑時，預設行為
          有可能一併改變且不會有任何參數錯誤
```

```
【來源】b10625 | 0cc5b14 | https://github.com/ggml-org/llama.cpp/pull/27679
【召回方式】精確關鍵字（Qwen 類別2、chat template 類別5）
【類型】⚠️風險　【等級】🟡
【summary】將 qwen3-coder 的 chat template workaround 限縮範圍
【判讀依據】backend-agnostic。我們不跑 qwen3-coder，但不帶 --jinja、吃預設
          chat template 處理，範圍限縮意味著預設行為在某些 Qwen 變體上會改變
```

```
【來源】b10662 | 1844325 | https://github.com/ggml-org/llama.cpp/pull/24124
【召回方式】精確關鍵字（類別5 參數定義變動、KV cache）
【類型】⚠️風險　【等級】🟡
【summary】新增 ctx-per-slot（--kv-unified-per-slot）
【判讀依據】backend-agnostic（server/core）。新增參數不影響我們現有的
          -c 65536，但它改變 unified KV cache 的 context 配置方式，
          需確認新增此選項後預設行為維持原樣
```

---

## ✨ 機會

### 🔴 需要決定（要不要採用/評估）

```
【來源】b10684 | cc83d7b | https://github.com/ggml-org/llama.cpp/pull/27629
【召回方式】模糊主題 O1、精確關鍵字（類別5）
【類型】✨機會　【等級】🔴
【summary】SYCL 上 --fit 改為計入指定 context 下的實際 peak VRAM；Arc b70
          實測 262144 context 完整可用
【判讀依據】原文："properly accounting for how much VRAM is required when the
          allocated context is fully used ... as well as preventing some
          overly-conservative calculations"
          **backend 判定：這個改進本身是 SYCL-scoped，不在我們路徑上。**
          但它讓 `--fit` 這個上游機制浮現——那正是我們自己在腳本裡硬編碼的
          事（>21GiB 全 offload、否則 -ngl 19）。
          **要動的手不是「採用這個 commit」，而是「確認 `--fit` 在
          Vulkan / CPU 路徑上能不能用」。** 若能用，我們可以移除
          --list-devices 解析＋門檻硬編碼，連帶消滅 R1 這整個高風險依賴
          （也就是上面 b10594 的根因）。**本期最值得投資的一筆。**
          相關：b10584 是 --fit 的另一項改進（非 backend-scoped）
```

```
【來源】b10731 | 0eadefe | https://github.com/ggml-org/llama.cpp/pull/28123
【召回方式】精確關鍵字（Qwen 類別2、推測解碼 類別3）
【類型】✨機會　【等級】🔴
【summary】qwen4exp（Qwen3.8-Flash-Next）新增 recurrent state rollback
          支援；缺此支援時 MTP 推測解碼每輪都要序列化整份 recurrent state，
          代價超過 drafting 的收益
【判讀依據】backend-agnostic（core）。原文："Without rollback support the
          context is classified as SEQ_RM_TYPE_FULL and the server
          serializes the whole recurrent state ... which costs more than
          the drafting saves" → 同時命中類別2 與類別3，是兩個追蹤項目的
          交集。相依：架構本身由 b10660 新增
```

```
【來源】b10672 | 511f9c1 | https://github.com/ggml-org/llama.cpp/pull/27843
【召回方式】精確關鍵字（Qwen3.5 類別2、OpenVINO 類別4）
【類型】✨機會　【等級】🔴
【summary】OpenVINO 升至 2026.3.1，支援 whisper.cpp，Qwen3.5 可於 NPU 執行，
          新增多個 op
【判讀依據】**backend 判定：第3步——OpenVINO 是我們的 fallback 路徑**
          （Vulkan 不可用時走它）。原文 "openvino : enable qwen35 on NPU"
          → 我們的 model（類別2 Qwen）＋我們的路徑（OpenVINO）＋我們機台的
          NPU 三者同時對上。原文亦說明 static shape 限制（-np 1 時的 s_copy
          處理），驗證時需注意
```

```
【來源】b10665 | ca3d5a3 | https://github.com/ggml-org/llama.cpp/pull/27804
【召回方式】精確關鍵字（Nemotron，類別2）
【類型】✨機會　【等級】🔴
【summary】新增 Nemotron3.5 的 DSpark 支援
【判讀依據】backend-agnostic（model 架構）。命中類別2 的 Nemotron 3.5
          （評估中的 unsloth_NVIDIA-Nemotron-3.5-Lightning-30B-A3B-UD-Q4_K_M）。
          上游首次支援 = 可以開始跑分了，需決定是否排入評估
```

```
【來源】b10660 | 6c84c7d | https://github.com/ggml-org/llama.cpp/pull/27742
【召回方式】精確關鍵字（Qwen，類別2）
【類型】✨機會　【等級】🔴
【summary】新增 Qwen3.8-Flash-Next（qwen4exp）架構與轉換器，含 low-rank
          hyper-connection 與 PLE n-gram hash embedding
【判讀依據】backend-agnostic（model 架構＋轉換器）。命中類別2 Qwen 系列的新
          架構，需決定是否納入評估。註：原文 50321 字，節錄依「具名實體不得
          捨棄」保留架構名與 tensor 名。後續：b10678、b10712、b10730、b10731
```

```
【來源】b10658 | b10f9ca | https://github.com/ggml-org/llama.cpp/pull/27342
【召回方式】精確關鍵字（DFlash，類別3）
【類型】✨機會　【等級】🔴
【summary】新增 DFlash2 推測解碼支援（local convolution + candidate
          selector），含 p_min 參數
【判讀依據】backend-agnostic（spec decode core）。命中類別3 我們明確追蹤的
          DFlash。相關：b10701 修 NVFP4 路徑、b10715 encoder 融合
```

```
【來源】b10653 | fac889f | https://github.com/ggml-org/llama.cpp/pull/27794
【召回方式】模糊主題 O2、精確關鍵字（類別5）
【類型】✨機會　【等級】🔴
【summary】model loader 新增 TENSOR_READ_LAZY 與 --tensor-read-lazy，tensor
          改為延遲讀取
【判讀依據】backend-agnostic（model loader）。延遲讀取直接影響模型載入時間，
          而端側機器的載入時間是使用者站在前面等的。符合 O2 Counts(→🔴)。
          **採用時請用改名後的 `--lazy-mode` / `-lzm`**（見 b10700），
          另見 b10705 的處理改善
```

```
【來源】b10645 | c5fc7e3 | https://github.com/ggml-org/llama.cpp/pull/26622
【召回方式】模糊主題 O1、精確關鍵字（類別5）
【類型】✨機會　【等級】🔴
【summary】新增 --n-cpu-ffn，將前 N 層 dense FFN 權重 offload 到 CPU；並
          整併 --n-cpu-moe 的 override 邏輯
【判讀依據】backend-agnostic（arg + offload 策略）。比 -ngl 19 更細緻的
          offload 手段，直接對上 8-32GB VRAM 的取捨。符合 O1 Counts(→🔴)：
          可在啟動腳本中採用的新參數
```

### 🟡 升版自動拿到，知道就好

| 來源 | summary | 召回方式 | 判讀依據（含 backend 判定） |
|---|---|---|---|
| b10730 | qwen4exp indexer head reduction 改依 slice 相加；55k context prompt processing 2170→2366 t/s | 精確關鍵字（Qwen 類別2） | agnostic。+9% 低於 signal 門檻，無東西可採用 |
| b10724 | KV cache 還原非連續 cell 改為批次 scatter read；原本 40k+ token 還原耗 25-63 秒 | 精確關鍵字（KV cache）、O2 | agnostic core。幅度極大但我們未用 prompt cache，無參數可採用 |
| b10715 | DFlash encoder 融入 KV injection 的 decode | 精確關鍵字（DFlash、KV cache） | agnostic。採用 DFlash 後才有意義 |
| b10714 | Vulkan 於 RDNA3 對 4 欄以上採靜態 4 rows 的 mat-vec；Strix Halo 實測較快 | 精確關鍵字（Strix Halo 類別1、Vulkan） | **Vulkan = 我們的路徑**，且直接命中我們的機台，但屬已支援硬體的調優，升版即得 |
| b10712 | Vulkan 新增 k>=1024 的 top_k radix select shader，針對 Qwen3.8 Flash Next | 精確關鍵字（Vulkan、Qwen） | **Vulkan = 我們的路徑**；隨 b10660 架構評估一併考慮 |
| b10707 | KV cell sequence 掃描提早結束；55k context generation 56.3→74.3 t/s、132k 33.6→50.9 t/s | 精確關鍵字（KV cache） | agnostic core。+32%/+51% 且 55k 正在我們區間，但純吞吐量、升版即得。**可作為排定跳版優先度的依據** |
| b10705 | 改善 TENSOR_READ_LAZY 處理；lazy 開啟時強制 tensor 留在 CPU | 模糊主題 O2 | agnostic loader。b10653 的後續 |
| b10701 | 修正 DFlash2 NVFP4 draft model 未傳遞 Q/K/V 與 output projection scale | 精確關鍵字（DFlash、NVFP4） | agnostic。需先決定採用 DFlash2 |
| b10678 | qwen4exp 減少 graph split 數量 | 精確關鍵字（Qwen） | agnostic。b10660 的後續 |
| b10675 | Vulkan shader 新增 row ID 與 expert count hoisting | 精確關鍵字（Vulkan） | **Vulkan = 我們的路徑**。MoE shader 內部優化，我們跑 A4B MoE 可受益 |
| b10649 | llama-server / llama-cli 新增僅供 benchmark 的合成推測接受率選項 | 精確關鍵字（spec decode 類別3） | agnostic。評估 MTP/DFlash 時可用 |
| b10603 | GLM-4.5-Air 新增 MTP 支援 | 精確關鍵字（MTP 類別3） | agnostic。MTP 機制成熟度訊號，非我們的 model |
| b10584 | --fit 計入 n_streams；draft context 跟隨 target context，修正 slot 超量時 server 回 500 | 精確關鍵字（類別5） | agnostic（非 backend-scoped，與 b10684 不同）。b10684 評估時一併看 |
| b10577 | 修復 draft-mtp 與 embeddings 併用的問題 | 精確關鍵字（draft model/MTP 類別3） | agnostic。類別3 機制可用性修復 |
| b10573 | mtmd 透過 ffmpeg 支援 webp 影像 | 模糊主題 R3 | agnostic。符合 R3 Counts(→🟡)：新增輸入格式，且引入 ffmpeg 外部依賴 |
| b10726 | AVX2 以批次 gemm 加速 IQ 量化的大 batch prompt processing | 精確關鍵字（i-quant 類別4） | **CPU = 我們的路徑**（x64 也吃 CPU 層）。但我們用 Q4_K 非 IQ，受益有限 |

---

## 自主判斷提報

本期無（36 筆皆由精確關鍵字或模糊主題召回）。

---

## 本期綜合觀察

**1. 比前一版少了 20 筆，全部是「不在我們執行路徑上的 backend」。**
角色 prompt 新增了「哪些 backend 才是我們的」判讀規則：我們不自行 build，
只跑官方 `win-vulkan-x64` / `win-cpu-arm64` binary 加 OpenVINO fallback。
被丟掉的（判讀為 ⚪）：

| backend | 丟掉的筆數 | 代表 |
|---|---|---|
| SYCL | 6 | 含前一版誤判為 ⚠️🔴 的 **b10717**（Intel GPU free memory）|
| Hexagon/HTP | 5 | 含前一版誤補的 b10643 |
| CUDA-only | 4 | b10704、b10718、b10728 |
| ROCm/HIP | 4 | b10659、b10703、b10720 |
| OpenCL/Adreno | 3 | 含 b10687（prefill +25%）|

**b10717 的修正最關鍵**：它只改 SYCL backend 取得 Intel GPU 記憶體的方式，
而我們 Intel 機台走 Vulkan / OpenVINO——`--list-devices` 印出的記憶體數字
不是從 SYCL 來的。前一版把它列為必須驗證的 ⚠️🔴，是錯的。

**2. b10610 被丟掉，這裡說明一下為什麼。**
「縮短 CUDA 與 Metal 的 virtual device 命名字串」乍看正中 R1（裝置列舉輸出
變動），但改動**限定在 CUDA 與 Metal**，兩者都不在我們路徑上，所以我們的
binary 印出的內容不變 → ⚪。若日後同樣的命名慣例延伸到 Vulkan，那時才算。

**3. Vulkan 反而變重要了。** 收斂後 ⚠️ 這一側有 3 筆是 Vulkan
（b10677 🔴、b10681 🟡、b10639 🟡），因為它是我們 x64 機台的唯一 GPU 路徑。
其中 b10677 的「靜默錯誤輸出、每次啟動結果不同、無 log」是我們**現在釘的
版本就可能有的問題**，不是跳版風險——這是跳版的**理由**。

**4. b10684 仍是最值得投資的一筆，但要做的事變了。** 它的改進是
SYCL-scoped，所以不能直接拿。真正的行動是「**確認 `--fit` 在 Vulkan / CPU
路徑上能不能用**」——若能，就可以移除 `--list-devices` 解析與 21GiB 門檻
硬編碼，連帶消滅 b10594 那條 ⚠️🔴 的根因。**一次投資解掉一個結構性弱點。**

**5. ARM 機台只跑 CPU，這件事值得單獨確認。**
`win-cpu-arm64` 意味著 Glymur / N1X 上沒有 GPU 或 NPU 加速。b10630
（KleidiAI 建置整合）因此變成 ⚠️🟡——KleidiAI 是 ARM CPU 唯一的優化來源，
建置改動若讓它沒被編進官方 binary，Arm 機台效能會靜默退步。

**6. 有一塊看不見的東西。** b10620 `sync : ggml` 無任何內容可判讀，
需人工展開該 commit range。
