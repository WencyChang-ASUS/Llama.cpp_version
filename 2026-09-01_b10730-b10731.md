# llama.cpp 版本差異報告 b10729 → b10731

【覆蓋量】本次掃描 2 筆 release，2 筆入選（⚠️風險 0 筆／✨機會 2 筆）

---

## ⚠️ 風險

### 🔴 需要動手（改腳本或驗證）

本期無。

### 🟡 留意

本期無。

---

## ✨ 機會

### 🔴 需要決定（要不要採用/評估）

```
【來源】b10731 | 0eadefe | https://github.com/ggml-org/llama.cpp/pull/28123
【召回方式】精確關鍵字（Qwen 類別2、推測解碼 類別3）
【類型】✨機會　【等級】🔴
【summary】qwen4exp（Qwen3.8-Flash-Next）新增 recurrent state rollback
          支援；缺此支援時 MTP 推測解碼每輪都要序列化整份 recurrent state，
          代價超過 drafting 的收益
【判讀依據】原文："MTP speculative decoding needs the target state to move
          back by the number of rejected draft tokens. Without rollback
          support the context is classified as SEQ_RM_TYPE_FULL and the
          server serializes the whole recurrent state to host memory on
          every round, which costs more than the drafting saves"
          → 同時命中類別2（我們跑過的 Qwen 系列）與類別3（MTP/推測解碼機制）。
          這一筆把「MTP 在 qwen4exp 上其實划不來」變成「可行」，等於解鎖
          我們兩個追蹤項目的交集。符合 ✨🔴：需要決定是否納入評估。
          相依：該架構本身由 b10660 新增（見 reports/b10567-b10729.md 的
          ✨🔴），本筆是讓它與 MTP 併用的前提條件
```

### 🟡 升版自動拿到，知道就好

```
【來源】b10730 | 09412af | https://github.com/ggml-org/llama.cpp/pull/28023
【召回方式】精確關鍵字（Qwen 類別2）
【類型】✨機會　【等級】🟡
【summary】qwen4exp 的 indexer head reduction 改為依 slice 相加，移除
          transpose 與 sum_rows；RTX PRO 6000 上 55k context prompt
          processing 2170 至 2366 t/s
【判讀依據】幅度約 +9%，低於角色設定的 signal 門檻（「a few percent is
          noise; tens of percent ... is signal」）；且無新參數可採用、
          升版即得 → 符合 ✨🟡。55k 落在我們 -c 65536 的區間，可作為
          升版優先度的參考。原文亦說明增益隨 context 與 ubatch size 變大
```

---

## 自主判斷提報

本期無（2 筆皆由精確關鍵字召回）。

---

## 本期綜合觀察

**1. 兩筆都在 qwen4exp 這條線上。** 接續上一期 b10660（新增架構）→ b10678（減少 graph split）→ b10712（Vulkan top_k），本期再加 b10730（indexer reduction）與 b10731（recurrent state rollback）。上游正在密集打磨這個架構，若要評估 Qwen3.8-Flash-Next，建議等這波收斂或至少納入 b10731。

**2. b10731 是兩個追蹤項目的交集。** 類別2（Qwen 系列）× 類別3（MTP）。單看任一清單都只是「又一筆 Qwen 相關」或「又一筆 MTP 相關」，是交集讓它值得 🔴。

**3. 本期沒有 ⚠️。** 上一期 5 筆 ⚠️🔴 的驗證工作（`--list-devices` 三筆 + mtmd resize 一筆 + Vulkan view-alias 一筆）仍然未消，本期沒有新增。
