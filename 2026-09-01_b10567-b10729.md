# llama.cpp 版本差異報告 b10566 → b10729

【覆蓋量】本次掃描 124 筆 release，45 筆入選（⚠️風險 15 筆／✨機會 30 筆）

---

## ⚠️ 風險

### 🔴 需要動手（改腳本或驗證）

5 筆中有 3 筆（b10594、b10717、b10610）指向同一件事：**llama.cpp 的裝置列舉與記憶體回報正在被積極改動**，而我們的啟動腳本用硬編碼 regex 解析 `--list-devices` 輸出、且沒有備援偵測方法。建議合併驗證。

```
【來源】b10594 | ba8e0ed | https://github.com/ggml-org/llama.cpp/pull/26692
【召回方式】模糊主題 R1
【類型】⚠️風險　【等級】🔴
【summary】device_info 迴圈改為僅在 verbosity 達 TRACE 時執行；該迴圈會建立
          GPU context 並查詢各裝置記憶體（CUDA 上配置 550MB VRAM）
【判讀依據】原文："the log verbosity must be set to LOG_LEVEL_TRACE. If it's
          not, including in the default configuration, the contexts get
          created, memory sizes get queried, then the log function quietly
          discards the data" → 我們的腳本靠 --list-devices 輸出的
          (N MiB, N MiB free) 偵測 VRAM，啟動時帶 -lv 3。若記憶體查詢在非
          TRACE 下被跳過，regex 抓不到數字 → maxMB=0 → 完全不帶 -ngl
          （fail-open）→ 小 VRAM 機台會嘗試全部 offload。
          符合 R1 Counts(→🔴)：該行在某條件下被丟棄
```

```
【來源】b10717 | a32af33 | https://github.com/ggml-org/llama.cpp/pull/27968
【召回方式】模糊主題 R1
【類型】⚠️風險　【等級】🔴
【summary】改用 Level Zero 與 SYCL API 取得 Intel GPU 可用記憶體，新增
          GGML_SYCL_GET_MEM_API 環境變數
【判讀依據】原文："Enhance to get the free memory of Intel GPU"、"enhance get
          mem info by l0 an SYCL API" → 改變 Intel GPU 回報 free memory 的
          來源與數值。我們出 Intel PTL 機台，-ngl auto 的 21GiB 門檻直接吃
          這個數字。符合 R1 Counts(→🔴)：該行的內容變動
```

```
【來源】b10610 | a14dba6 | https://github.com/ggml-org/llama.cpp/pull/27608
【召回方式】模糊主題 R1
【類型】⚠️風險　【等級】🔴
【summary】縮短 CUDA 與 Metal 的 virtual device 命名字串；Metal 改在 init
          時建立 device description
【判讀依據】原文："shorten virtual device naming in CUDA and Metal" → 直接
          改動裝置列舉的輸出文字。我們的 regex 只匹配記憶體括號段，理論上
          不受名稱影響，但同一份輸出被改動就必須實測確認括號段未受牽連。
          符合 R1 Counts(→🔴)：措辭/結構變動
```

```
【來源】b10677 | b387ddf | https://github.com/ggml-org/llama.cpp/pull/27812
【召回方式】精確關鍵字（Vulkan，類別4）
【類型】⚠️風險　【等級】🔴
【summary】修正 ggml_vk_graph_optimize 漏掉 view-alias 依賴，導致節點跨
          aliased 讀寫被重排
【判讀依據】原文："silently wrong tokens under greedy decoding, different
          output on every server start, and invalid speculative-decoding
          acceptance, with nothing logged. Hits Qwen3.8's recurrent state
          (and any model with view-aliased state) on AMD and NVIDIA Vulkan"
          → Vulkan 是我們 AMD/Intel 機台的主力後端，錯誤是靜默的、且每次
          啟動輸出都不同。必須確認自己版本是否受影響。
          註：這是 bug 修復，若機械套用「一般 bug 修復 → ⚪」會被丟掉
```

```
【來源】b10598 | 56db501 | https://github.com/ggml-org/llama.cpp/pull/27594
【召回方式】模糊主題 R3
【類型】⚠️風險　【等級】🔴
【summary】mtmd 改用 pillow 精確的 resize 演算法，並修正各 model 的
          resize_algo
【判讀依據】原文："use pillow-accurate algo, correct resize_algo for all
          models" → 我們偵測到 mmproj 就一律走多模態路徑。影像 resize
          演算法改變會直接改變我們的多模態輸出，而**沒有任何參數變動、
          沒有任何錯誤訊息**。
          符合 R3 Counts(→🔴)：既有 model 的預處理行為改變，且措辭正是
          「correct ... for all models」
```

### 🟡 留意

```
【來源】b10728 | e4b9af0 | https://github.com/ggml-org/llama.cpp/pull/25635
【召回方式】agent自主判斷
【類型】⚠️風險　【等級】🟡
【summary】CUDA flash-attn 的 K/V shared memory fp16 tile 改用 XOR swizzle；
          含修正 DGX Spark 上 FA 的 shared memory race
【判讀依據】原文步驟含 "fix shared memory race in FA on DGX Spark"。
          DGX Spark 即 GB10 / Grace Blackwell，對應 keywords 類別1 追蹤中的
          NVIDIA N1X。這是我們計畫出貨硬體上的**資料競爭**（靜默錯誤來源）。
          目前未出貨故非 🔴，但列為必須知道的既知問題。
          清單缺口：類別1 的 N1X 關鍵字有 "GB10"/"Grace Blackwell" 但沒有
          "DGX Spark"，因此精確關鍵字未命中
```

```
【來源】b10700 | bebc935 | https://github.com/ggml-org/llama.cpp/pull/27969
【召回方式】精確關鍵字（類別5，參數定義變動）
【類型】⚠️風險　【等級】🟡
【summary】--tensor-read-lazy 改名為 --lazy-mode，新增 -lzm 短旗標
【判讀依據】參數改名，但該參數是本期才新增（b10653）、我們尚未使用，目前
          無影響。列 🟡 的理由：證實 llama.cpp 會改名參數，且短旗標命名
          空間在擴張（我們依賴 -m / -c / -lv 的短形式）
```

```
【來源】b10620 | 81191af | PR: (無)
【召回方式】內容不明（Stage 1 標記）
【類型】⚠️風險　【等級】🟡
【summary】（內容不明：sync : ggml，無 PR 編號）
【判讀依據】節錄僅有 "sync : ggml" 兩個字、無 PR 編號。這類 bulk merge 可能
          攜帶大量 kernel/後端/記憶體改動，但 release body 不提供任何訊號。
          **不假裝判讀得出來** → 需人工展開該 commit range 確認
```

```
【來源】b10690 | bdf3955 | https://github.com/ggml-org/llama.cpp/pull/27967
【召回方式】精確關鍵字（KV cache，類別4）
【類型】⚠️風險　【等級】🟡
【summary】僅在 k_rot tensor 已配置 buffer 時複製 Hadamard 矩陣，避免未量化
          K cache 於 context shift 時 crash
【判讀依據】原文 "prevent crashes during context shift of unquantized K
          cache" → 我們不帶 -ctk，用預設的未量化 K cache，且帶 -c 65536，
          長對話可能觸發 context shift。需確認我們的版本是否在此路徑上
```

```
【來源】b10659 | 6fdd0ac | https://github.com/ggml-org/llama.cpp/pull/26973
【召回方式】精確關鍵字（ROCm，類別4）
【類型】⚠️風險　【等級】🟡
【summary】Windows ROCm release 改為打包 amdhip64_7/amd_comgr/rocm_kpack，
          避免載到 System32 的驅動版本
【判讀依據】原文 "so the correct HIP runtime loads over the driver's copy in
          System32" → 我們出 AMD Strix Halo + Windows 機台。若使用官方
          release binary，runtime 載入行為會改變；自行 build 則不受影響。
          需確認我們的取得方式
```

```
【來源】b10639 | 5e6a37c | https://github.com/ggml-org/llama.cpp/pull/27726
【召回方式】精確關鍵字（Vulkan，類別4）
【類型】⚠️風險　【等級】🟡
【summary】Vulkan warptile 原假設 warp size <= 64，對更大 warp 加入 clamp
          workaround
【判讀依據】"warptiles currently assume warp sizes <= 64" → 這是相容性
          workaround 而非最佳化，暗示某些裝置的 warp size 超出原假設。
          我們的 AMD/Intel 機台走 Vulkan，屬相容性風險但未點名我們的硬體
```

```
【來源】b10585 | d9f918d | https://github.com/ggml-org/llama.cpp/pull/27511
【召回方式】精確關鍵字（類別5，jinja）
【類型】⚠️風險　【等級】🟡
【summary】新增 common/json 抽象層，server 與 jinja 全面改用，含一處 server
          crash 修復
【判讀依據】原文步驟含 "adapt jinja"、"migrate server"、"fix server crash"
          → 我們不帶 --jinja，chat template 完全吃 llama.cpp 預設行為
          （類別5「刻意不帶、吃預設的參數」）。大規模重構觸及 jinja 路徑時，
          預設行為有可能一併改變且不會有任何參數錯誤
```

```
【來源】b10625 | 0cc5b14 | https://github.com/ggml-org/llama.cpp/pull/27679
【召回方式】精確關鍵字（Qwen 類別2、chat template 類別5）
【類型】⚠️風險　【等級】🟡
【summary】將 qwen3-coder 的 chat template workaround 限縮範圍
【判讀依據】命中類別2 的 Qwen，且動到 chat template。我們不跑 qwen3-coder，
          但我們不帶 --jinja、吃預設 chat template 處理，範圍限縮意味著
          預設行為在某些 Qwen 變體上會改變
```

```
【來源】b10662 | 1844325 | https://github.com/ggml-org/llama.cpp/pull/24124
【召回方式】精確關鍵字（類別5 參數定義變動、KV cache）
【類型】⚠️風險　【等級】🟡
【summary】新增 ctx-per-slot（--kv-unified-per-slot）
【判讀依據】新增參數，不影響我們現有的 -c 65536。但它改變 unified KV cache
          的 context 配置方式，需確認新增此選項後預設行為是否維持原樣
```

```
【來源】b10617 | 814d84b | https://github.com/ggml-org/llama.cpp/pull/27660
【召回方式】精確關鍵字（quant，類別4）
【類型】⚠️風險　【等級】🟡
【summary】SYCL 將 tq2_0 標記為不支援
【判讀依據】某量化格式在 Intel/SYCL 路徑上被標為不支援。我們出 Intel PTL
          機台，但目前未使用 tq2_0，影響為零；列 🟡 是因為「某格式在某後端
          被移除支援」這個模式值得留意
```

---

## ✨ 機會

### 🔴 需要決定（要不要採用/評估）

```
【來源】b10684 | cc83d7b | https://github.com/ggml-org/llama.cpp/pull/27629
【召回方式】模糊主題 O1、精確關鍵字（類別5）
【類型】✨機會　【等級】🔴
【summary】SYCL 上 --fit 改為計入指定 context 下的實際 peak VRAM；Arc b70
          實測 262144 context 完整可用
【判讀依據】原文 "properly accounting for how much VRAM is required when the
          allocated context is fully used (which makes the reported context
          drop below what it did before, but stop it OOMing) as well as
          preventing some overly-conservative calculations"
          → 這正是我們自己在腳本裡硬編碼的那件事（>21GiB 全 offload、否則
          -ngl 19）。符合 O1 Counts(→🔴)：可取代我們現在硬編碼的邏輯。
          **本期最值得評估的一筆**：若 --fit 足夠可靠，我們可以移除
          --list-devices 解析＋門檻硬編碼，連帶消滅上面 3 筆 ⚠️🔴 的根因。
          相關前置改進見 b10584
```

```
【來源】b10645 | c5fc7e3 | https://github.com/ggml-org/llama.cpp/pull/26622
【召回方式】模糊主題 O1、精確關鍵字（類別5）
【類型】✨機會　【等級】🔴
【summary】新增 --n-cpu-ffn，將前 N 層 dense FFN 權重 offload 到 CPU；並
          整併 --n-cpu-moe 的 override 邏輯
【判讀依據】"add --n-cpu-ffn to CPU-offload dense FFN weights of first N
          layers" → 比 -ngl 19 更細緻的 offload 手段，直接對上 8-32GB VRAM
          的取捨。符合 O1 Counts(→🔴)：可在啟動腳本中採用的新參數
```

```
【來源】b10653 | fac889f | https://github.com/ggml-org/llama.cpp/pull/27794
【召回方式】模糊主題 O2、精確關鍵字（類別5）
【類型】✨機會　【等級】🔴
【summary】model loader 新增 TENSOR_READ_LAZY 與 --tensor-read-lazy，tensor
          改為延遲讀取
【判讀依據】延遲讀取 tensor 直接影響模型載入時間，而端側機器的載入時間是
          使用者站在前面等的。符合 O2 Counts(→🔴)：可採用的新參數。
          注意：此參數已於 b10700 改名為 --lazy-mode（短旗標 -lzm），
          採用時請用新名稱；另見 b10705 的處理改善
```

```
【來源】b10672 | 511f9c1 | https://github.com/ggml-org/llama.cpp/pull/27843
【召回方式】精確關鍵字（Qwen3.5，類別2）
【類型】✨機會　【等級】🔴
【summary】OpenVINO 升至 2026.3.1，支援 whisper.cpp，Qwen3.5 可於 NPU 執行，
          新增多個 op
【判讀依據】原文 "openvino : enable qwen35 on NPU" → 我們的 model（類別2
          Qwen 系列）＋我們的硬體（PTL/N1X 帶 NPU）同時對上。符合 🔴 條件：
          需要驗證的新組合。原文亦說明 static shape 相關限制
          （-np 1 時的 s_copy 處理），驗證時需注意
```

```
【來源】b10660 | 6c84c7d | https://github.com/ggml-org/llama.cpp/pull/27742
【召回方式】精確關鍵字（Qwen，類別2）
【類型】✨機會　【等級】🔴
【summary】新增 Qwen3.8-Flash-Next（qwen4exp）架構與轉換器，含 low-rank
          hyper-connection 與 PLE n-gram hash embedding
【判讀依據】命中類別2 Qwen 系列的新架構，需決定是否納入評估。
          註：原文 50321 字，節錄依「具名實體不得捨棄」規則保留架構名、
          tensor 名與轉換路徑；完整內容見 PR。相關後續：b10678 減少 graph
          split、b10712 Vulkan top_k 支援
```

```
【來源】b10658 | b10f9ca | https://github.com/ggml-org/llama.cpp/pull/27342
【召回方式】精確關鍵字（DFlash，類別3）
【類型】✨機會　【等級】🔴
【summary】新增 DFlash2 推測解碼支援（local convolution + candidate
          selector），含 p_min 參數
【判讀依據】命中類別3（MTP/推測性解碼機制）中我們明確追蹤的 DFlash。
          新機制需決定是否評估。相關：b10701 修復其 NVFP4 路徑、
          b10715 將 encoder 融入 KV injection、b10718 CUDA MoE fusion
          擴及推測解碼
```

### 🟡 升版自動拿到，知道就好

```
【來源】b10707 | 62acc89 | https://github.com/ggml-org/llama.cpp/pull/28011
【召回方式】精確關鍵字（KV cache，類別4）
【類型】✨機會　【等級】🟡
【summary】KV cell 的 sequence 掃描在看完該 cell 所屬 sequence 後即停止；
          RTX PRO 6000 上 55k context generation 56.3 至 74.3 t/s、
          132k 33.6 至 50.9 t/s
【判讀依據】幅度符合角色定義的 signal（+32% / +51%），且 55k 正落在我們
          -c 65536 的使用區間。但這是純吞吐量改善、升版即得，沒有東西要
          決定 → 🟡。可作為排定升版優先度的依據
```

```
【來源】b10724 | 2d8d612 | https://github.com/ggml-org/llama.cpp/pull/27991
【召回方式】精確關鍵字（KV cache，類別4）、模糊主題 O2
【類型】✨機會　【等級】🟡
【summary】KV cache 還原非連續 cell 時改為按連續區段批次 scatter read；
          原本 40k+ token 還原需約 1.4M 次小 copy、CUDA 上耗 25 至 63 秒
【判讀依據】原文 "~1.4M copies of a few KiB each for a 40k+ token restore,
          taking 25-63 s on the CUDA backend"。幅度極大且落在我們 -c 65536
          的量級。但我們未使用 prompt cache，且無新參數要採用，升版即得
          → 符合 O2 Counts(→🟡)
```

本段其餘 22 筆為後端調優、內部效能改善、或不涉及我們 model 與硬體的優化，
升版即自動獲得：

| 來源 | summary | 召回方式 | 判讀依據 |
|---|---|---|---|
| b10714 | Vulkan 於 RDNA3 對 4 欄以上採靜態 4 rows 的 mat-vec 設定；Strix Halo 實測較快 | 精確關鍵字（Strix Halo，類別1） | 直接命中我們的機台，但屬「已支援硬體的調優」，符合 O3 Counts(→🟡) |
| b10703 | 調校 RDNA3 的 mmq 設定 | 精確關鍵字（類別1，RDNA3 涵蓋 gfx115x） | 同上，我們現有這條線的調優 |
| b10720 | ROCm 新增長 row 的 radix TOP_K | 精確關鍵字（ROCm，類別4） | AMD 後端優化，我們的 Strix Halo 機台受益，升版即得 |
| b10723 | OpenCL 針對 Intel Xe-LP GPU 調校量化路徑，改善 TG 與 PP | 精確關鍵字（OpenCL、quant 類別4） | Xe-LP 為較舊世代 iGPU，我們是 PTL；符合 O3 Does NOT count 的舊世代，但 quant/OpenCL 命中類別4 故仍列 🟡 |
| b10715 | DFlash encoder 融入 KV injection 的 decode，省去 device-host 往返與第二次 graph build | 精確關鍵字（DFlash 類別3、KV cache 類別4） | DFlash 內部效率改善，僅在採用 DFlash 後才有意義 |
| b10701 | 修正 DFlash2 NVFP4 draft model 未傳遞 Q/K/V 與 output projection scale | 精確關鍵字（DFlash 類別3、NVFP4 類別1/4） | 讓 DFlash2+NVFP4 真正可用，但需先決定採用 DFlash2（見 b10658） |
| b10718 | CUDA 將 MoE fusion 擴及推測解碼；原僅限單 token | 精確關鍵字（CUDA 類別4、spec decode 類別3） | 推測解碼的 CUDA 融合放寬，採用 DFlash/MTP 後才受益 |
| b10669 | SYCL oneDNN SDPA 路徑改為就地綁定 f16 KV cache，省去每次 call 142.6MB staging | 模糊主題 O1、精確關鍵字（KV cache） | Intel 路徑記憶體改善且有具體數字，但屬後端內部改動、無需採用動作 |
| b10670 | Xe2（BMG）上量化 KV decode 改走 TILE，其他架構維持 VEC | 精確關鍵字（KV cache） | 限 Xe2/BMG，我們是 Xe1/PTL，暫不適用 |
| b10704 | CUDA 放寬 mm_ids_helper 快速路徑條件，任何 n_expert_used 皆可走 | 精確關鍵字（CUDA，類別4） | MoE 快速路徑放寬，RTX 系列受益，升版即得 |
| b10712 | Vulkan 新增 k >= 1024 的 top_k radix select shader，針對 Qwen3.8 Flash Next | 精確關鍵字（Vulkan、Qwen） | 針對 b10660 新架構的 Vulkan 支援，隨該架構評估一併考慮 |
| b10678 | qwen4exp 減少 graph split 數量 | 精確關鍵字（Qwen 相關） | 同上，b10660 架構的後續優化 |
| b10675 | Vulkan shader 新增 row ID 與 expert count hoisting | 精確關鍵字（Vulkan） | MoE shader 內部優化 |
| b10705 | 改善 TENSOR_READ_LAZY 處理；lazy 開啟時強制 tensor 留在 CPU | 模糊主題 O2 | b10653 的後續改善，採用該功能時一併留意 |
| b10726 | AVX2 上以批次 gemm 加速 IQ 系列量化的大 batch prompt processing | 精確關鍵字（i-quant，類別4） | CPU 路徑；-ngl 19 時部分層在 CPU，可間接受益 |
| b10702 | HIP 於 gfx1201 以原生 amdgcn perm 優化 Q2_0 dot product 與 MMQ unpack | 模糊主題 O3 | gfx1201（RDNA4）比我們的 gfx115x 新，但屬既有支援硬體的優化 |
| b10603 | GLM-4.5-Air 新增 MTP 支援 | 精確關鍵字（MTP，類別3） | MTP 機制在更多 model 上落地，機制成熟度訊號，非我們的 model |
| b10577 | 修復 draft-mtp 與 embeddings 併用的問題 | 精確關鍵字（draft model/MTP，類別3） | 類別3 機制的可用性修復，升版即得 |
| b10649 | llama-server 與 llama-cli 新增僅供 benchmark 的合成推測接受率選項 | 精確關鍵字（spec decode，類別3） | benchmark 工具，評估 MTP/DFlash 時可用 |
| b10584 | --fit 計入 n_streams；draft context 改為跟隨 target context 大小，修正 slot 超量時 server 回 500 | 精確關鍵字（類別5） | b10684 的前置改進，本身不構成決定點 |
| b10576 | SYCL 重新加入 Q2_K reordered MMVQ 與 ESIMD kernel（前次曾被 revert），新增 gate 參數 | 精確關鍵字（quant 類別4） | Intel 路徑量化 kernel。「(again)」表示曾被 revert，穩定性待觀察 |
| b10573 | mtmd 透過 ffmpeg 支援 webp 影像 | 模糊主題 R3 | 符合 R3 Counts(→🟡)：新增輸入格式，且該路徑引入 ffmpeg 外部依賴 |

---

## 自主判斷提報

本期靠召3（兩份清單皆未召回）撈出 **1 筆**：

| 來源 | 判讀 | 為何清單接不住 | 建議 |
|---|---|---|---|
| b10728 | ⚠️🟡 DGX Spark 上 FA shared memory race 修復 | 類別1 的 NVIDIA N1X 有 "N1X"/"GB10"/"Grace Blackwell"，但沒有產品名 **"DGX Spark"** | 建議在類別1 的 N1X 列加入 "DGX Spark" 關鍵字 |

**上期的兩筆提報已晉升為正式清單項目**，本期起由清單直接召回：

| 上期提報 | 這次的處理 | 本期效果 |
|---|---|---|
| b10585 json.h 重構觸及 jinja | 類別5 加入 "jinja"/"chat template"/"chat_template" 召回詞 | b10585 改由精確關鍵字召回；b10625 亦多一路命中 |
| b10598 mtmd resize 演算法改變 | 新增模糊主題 **R3「多模態預處理行為變動」** | b10598 **由 ⚠️🟡 升為 ⚠️🔴**（R3 Counts(→🔴) 明確涵蓋「correct ... for all models」）；並新召回 b10573 |

---

## 本期綜合觀察

**1. R3 上線就抓到一筆 🔴。** b10598 上期只是召3 提報的 ⚠️🟡，加入 R3 後升為 ⚠️🔴——「所有 model 的影像 resize 演算法改變」會直接改變我們的多模態輸出，而且無參數變動、無錯誤訊息。R2 只保護那兩個 flag，保護不到這個。

**2. 四筆 ⚠️🔴 指向兩個根因。** 三筆是裝置列舉／記憶體回報（R1），一筆是多模態預處理（R3）。共同點：**我們都在解析或依賴上游的「行為」而非「介面」**，所以精確關鍵字接不住。

**3. b10684 可能一次解掉三筆。** 若 SYCL 的 `--fit` 足夠可靠，可整批移除「`--list-devices` 解析 + 21GiB 門檻 + `-ngl 19` fallback」這套自製邏輯，連帶消滅 R1 這個高風險依賴。建議把它的評估排在三筆 🔴 的驗證之前。

**4. lazy load 這條線本期共 4 筆連動**：b10653 新增 → b10679 bench 支援 → b10700 改名 `--lazy-mode` → b10705 處理改善。若要採用，直接用 `--lazy-mode` / `-lzm`。

**5. DFlash 這條線共 4 筆連動**：b10658 新增 DFlash2 → b10701 修 NVFP4 scales → b10715 encoder 融合 → b10718 CUDA MoE fusion 擴及推測解碼。若要評估，這四筆一起看。

**6. 有一塊看不見的東西。** b10620 `sync : ggml` 無任何內容可判讀，需人工展開該 commit range。
